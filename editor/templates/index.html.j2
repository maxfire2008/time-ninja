{% extends "base.html.j2" %}
{% block content %}
    <img src="{{ url_for('static', filename='wordmark.svg') }}"
         alt="Time Ninja"
         style="display: block;
                align-self: left;
                max-width: 100vw;
                max-height: 10vh">
    <small>© Max Burgess 2023
        <br>
    All rights reserved</small>
    <h2>Events</h2>
    <ul>
        {% for filename, data in files.items() %}
            <li>
                <a href="{{ url_for('event', slug=filename) }}">{{ filename }} {{ data }}</a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}
